name: Validate book

# Validation-only workflow. Runs `mdbook test` (rust doctest) and
# `mdbook build` on every push and PR that touches the book — catches
# broken markdown, dead links, malformed code fences.
#
# Publishing (HTML → gh-pages, PDF → GitHub Release asset) is driven
# locally by `make release` + `make release-publish` on the integrator's
# machine. That keeps the multi-GB pandoc + XeLaTeX + mermaid-cli
# toolchain off the runner — it lives in the tools/docker/mdbook image
# the integrator already has built locally.
#
# The pandoc backend is marked `optional = true` in book/book.toml so
# host-install mdbook (no pandoc) skips PDF rendering with a warning
# instead of failing the build.

on:
  push:
    branches: [main]
    paths:
      - 'book/**'
      - '.github/workflows/book.yml'
  pull_request:
    paths:
      - 'book/**'
      - '.github/workflows/book.yml'

# peaceiris/actions-mdbook@v2 (its latest major) still declares
# runs.using: node20 — there is no Node-24 release to bump to yet. Force
# the runner to execute Node-20 JS actions under Node 24 so this workflow
# stays off the deprecated runtime. Drop this once actions-mdbook ships a
# node24 release. (All other actions in the repo are already on node24.)
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup mdBook
        uses: peaceiris/actions-mdbook@v2
        with:
          mdbook-version: 'latest'

      - name: Install mdbook-mermaid
        # mdbook-mermaid is a separate preprocessor binary; peaceiris/actions-mdbook
        # only ships mdbook itself. Cached across runs.
        uses: baptiste0928/cargo-install@v3
        with:
          crate: mdbook-mermaid

      - name: Build HTML (catches malformed markdown + dead links)
        # Pandoc backend is `optional = true` in book.toml, so this
        # builds HTML cleanly without needing pandoc/XeLaTeX on the runner.
        #
        # `mdbook test` is deliberately NOT run here — it invokes rustdoc
        # on every untagged code fence, treating it as Rust. This book
        # has zero Rust code (it's a Go project's operator-facing docs;
        # actual languages used: bash / go / hcl / json / yaml / text /
        # mermaid / powershell), so the test step generated only false
        # positives. Dropped in v1.0.1's recovery cut.
        run: mdbook build book/
