# Sprint 6 — architect issues, resolution notes

Eleven issues filed: 2 medium (placeholder chapters 27 + 29), 9 low (forward-references / spot-check items). Resolutions split between integrator actions (running the staff's auto-generators; spot-checks against staff's landed code) and architect self-resolves (most of the low-severity items closed cleanly when the staff agent's deliverables landed).

## Issue 1 (MEDIUM — chapter 27 placeholder) — resolved by integrator (generator ran)

Staff landed `tools/refgen/cobra-md/main.go` + smoke tests in the same dispatch window. The integrator ran:

```bash
go run ./tools/refgen/cobra-md > book/src/27-command-reference.md
```

The placeholder content was replaced by a 1151-line auto-generated chapter covering all 25 top-level commands + global flags + per-subcommand flag tables with parent backlinks. Output starts with `# Command reference` and the verbatim "Auto-generated by `go run ./tools/refgen/cobra-md`. Re-run on every CLI surface change." callout per the architect's framing.

**Status**: ✅ resolved (chapter 27 is now the real generator output)

## Issue 2 (MEDIUM — chapter 29 placeholder) — resolved by integrator (generator ran)

Staff landed `tools/refgen/tfvars-md/main.go` + smoke tests. The integrator ran:

```bash
go run ./tools/refgen/tfvars-md > book/src/29-terraform-variable-reference.md
```

The placeholder was replaced by a 205-line auto-generated chapter covering the root module + 6 submodules with sensitive flags honored.

**Status**: ✅ resolved

## Issue 3 (LOW — chapter 23 CI workflow filename + doctor green-by-default) — accepted (validator + staff both landed their pieces)

Validator landed `.github/workflows/e2e-full.yml` (the manual-trigger + release-branch CI workflow). Staff landed the doctor green-by-default refactor (`internal/doctor/doctor.go` — `terraform` is the only required tool; kubectl/oc/ibmcloud/iperf3/dig are informational) pinned by `internal/doctor/doctor_test.go::TestHasFailures_StockDevBoxGreen`. Chapter 23's prose accurately describes both. No follow-up edit required.

**Status**: ✅ accepted (chapter 23 ↔ landed code consistent)

## Issue 4 (LOW — chapter 26 `ROKSBNKCTL_WORKSPACE` env var) — resolved by integrator

Verified via `grep -nE "ROKSBNKCTL_WORKSPACE" internal/cli/root.go internal/config/*.go` — no env-var binding exists. The actual workspace context is set by the `--workspace`/`-w` flag (or the active shell's last `roksbnkctl ws use`). Updated chapter 26's "Workspace-delete current-workspace gotcha" entry to drop the `ROKSBNKCTL_WORKSPACE` reference and describe the actual mechanism (`--workspace` flag + persistent ws-use context). The gotcha shape stays valid; the chapter is now correct against the binary.

**Status**: ✅ resolved (chapter 26 ↔ root.go consistent)

## Issue 5 (LOW — chapter 23 per-phase log path) — accepted (validator's path matches the chapter)

Verified validator's landed `scripts/e2e-test-backends.sh` + `scripts/e2e-test-full.sh` use `/tmp/roksbnkctl-e2e-backends/run-<ts>.log` and `/tmp/roksbnkctl-e2e-backends/<phase>-<ts>.log` — matches chapter 23's prose. No edit required.

**Status**: ✅ accepted

## Issue 6 (LOW — chapter 25 `cos object --multipart` flag) — accepted (chapter matches actual v1.0 behaviour)

Verified `internal/cli/cos.go` — neither `--multipart` nor `--stream` exist as flags. Chapter 25 already documents the actual auto-multipart behaviour ("transparent — there's no `--multipart` flag to set"). Chapter is correct.

**Status**: ✅ accepted

## Issue 7 (LOW — chapter 31 `goreleaser.yml` path) — resolved by integrator

Verified — the file lives at `.goreleaser.yml` (the conventional dotfile location), not `goreleaser.yml`. Updated chapter 31's link to point at `.goreleaser.yml`. The chapter's other `goreleaser` usages (the command examples) are correct.

**Status**: ✅ resolved

## Issue 8 (LOW — chapter 22 reorder anchor invariance) — accepted (anchor stable)

The reordered section's slug is derived from the header text (`#openshift-scc-failure-mode`) via mdbook's GFM-compatible slug algorithm — moving the section doesn't change the slug. Spot-checked the rest of the book for inbound `22-throughput-testing.md#openshift-scc-failure-mode` anchors; no cross-references found that would break. No edit required.

**Status**: ✅ accepted

## Issue 9 (LOW — chapter 26 doctor refresh assumption) — accepted (staff landed the refresh)

Staff landed the doctor green-by-default refactor in Sprint 6 (`internal/doctor/doctor.go` — `terraform` only required; kubectl/oc/ibmcloud/iperf3/dig informational) pinned by `internal/doctor/doctor_test.go`. Chapter 26's `doctor` entry is correct against the landed code.

**Status**: ✅ accepted

## Issue 10 (LOW — glossary "Cell" entry) — resolved by integrator (entry dropped)

Verified `grep -wE "Cell" book/src/*.md | grep -v 30-glossary.md` returns zero hits — the term isn't used anywhere else in the book. Dropped the entry from chapter 30; the rest of the glossary is unchanged.

**Status**: ✅ resolved

## Issue 11 (LOW — chapter 23 disk size estimate) — accepted (estimate is reasonable)

The 200 MB estimate in chapter 23 §"Pre-requisites" is a rough order-of-magnitude figure for a ROKS + BNK terraform state. Without a measurement against a real freshly-applied workspace (no IBM Cloud account on the sprint VM), the integrator's manual v1.0 sign-off run is the right place to refine. The chapter's framing ("approximately 200 MB; allow more for the `.terraform/` cache") is honest about the estimate-vs-measurement boundary. No Sprint-6 edit; deferred for v1.0 sign-off refinement.

**Status**: ⏸ accepted (estimate is reasonable; refine at v1.0 sign-off if needed)

## Integrator additions

- Ran staff's `tools/refgen/cobra-md` and `tools/refgen/tfvars-md` generators; chapters 27 + 29 are now real auto-generated content (1151 + 205 lines).
- Verified `go build/vet/gofmt/test` all green post-integration (including the new generator smoke tests + staff's doctor + cred-invariance tests).
- Verified DRY_RUN walkthroughs of both `scripts/e2e-test-backends.sh` and `scripts/e2e-test-full.sh` emit all phases cleanly (validator Issue 1 resolved).

## Summary

11 issues filed; 11 resolved (2 by running generators; 1 by editing chapter 26 wording; 1 by editing chapter 31 path; 1 by dropping a glossary entry; 6 self-resolved via spot-check against staff's/validator's landed code; 1 deferred to v1.0 sign-off as a reasonable estimate). Build, vet, gofmt, full test suite, and both DRY_RUN walkthroughs green.
