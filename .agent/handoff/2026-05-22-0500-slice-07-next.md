# Handoff — slices 01-06 shipped; slice 07 is next

**Written**: 2026-05-22 05:00 by Lead session
**Branch at write time**: main (clean)
**Trigger**: user-handoff (`/clear` to restart context)

## What was in flight

- Post-Terraform direction is now landed through slice 6: VPC + IAM + EKS + node group + kubeconfig + forge register + k8s foundation (namespaces, secrets, cert-manager, BNK cert chain) + FLO Helm install + OTEL certs. All 6 slices merged to main with full real-account validation on AWS account `292785712872` (ap-southeast-2).
- No work in flight. PR #14 (slice 6) merged at `b31695d`; AWS-side resources fully torn down (`aws ec2 describe-vpcs ... = 0 VPCs`).

## Next concrete action

Read `.agent/DECISIONS.md` (D-001..D-009), then `docs/POST_TERRAFORM_DIRECTION.md` §6/§13 to see what's NOT done — the slice 7+ scope. Pick the next slice with the user (most likely either CNEInstance/License activation OR `inspect`/`doctor`/`status` polish OR `terraform/`+`internal/tf/` removal).

## Pointers

- Active tasks: none
- Branches with uncommitted intent: none — main is clean
- Open PRs: none (PR #8/#9/#10/#11/#12/#13/#14 all merged)
- Relevant ADRs / docs:
  - `.agent/DECISIONS.md` — D-001..D-009 (architectural commitments)
  - `docs/POST_TERRAFORM_DIRECTION.md` — §6 phase ordering (current state), §7 down ordering, §13 slice roadmap
  - `docs/FORGE_MCP_INTEGRATION.md` — forge integration model (already implemented in slice 4)
  - `.agent/tasks/done/{slice-01..slice-06-flo-helm}/TASK.md` — full slice histories
- Decisions made this session worth remembering:
  - **Slice 5 scope correction**: dropped ECR mirror + S3 supply chain (TF holdovers; aws-gpu-setup doesn't use them). See `docs/POST_TERRAFORM_DIRECTION.md` §13 "Removed from the roadmap" block.
  - **Helm Go SDK** is now a top-level dep (added in slice 6) — `helm.sh/helm/v3@v3.21.0`. Don't bump back below v3.21 — earlier versions have CVEs.
  - **k8sfake.NewSimpleClientset** is widely used in test files with `//lint:file-ignore SA1019` directives; this is intentional, not a sign to migrate.

## What is NOT done

- **cloud-network-mapping ConfigMap** — needs TMM data-path subnets (BNK_EXT_CIDR / BNK_INT_CIDR) added to `cluster.yaml.Network`. Schema work.
- **NetworkAttachmentDefinitions** — pattern-specific (host-device vs sr-iov-tmm). `internal/k8s/manifests/{host-device,sr-iov-tmm}/` are empty scaffolds.
- **IRSA SA pre-creation** — needs OIDC provider IAM resource (`iam:CreateOpenIDConnectProvider`) — its own sub-task; aws-gpu-setup/up.sh has the canonical pattern.
- **CNEInstance CR** — the activation trigger; needs IRSA SA done first (image won't boot without `AWS_ROLE_ARN`).
- **License CR** — needs `licenses.k8s.f5net.com` CRD which CNEInstance reconcile installs (chicken-and-egg).
- **CWC DNS-warmup heal** — force-delete crash-looping cwc pod after 3 restarts; ported from aws-gpu-setup/deploy-bnk.sh:194-216.
- **F5SPKVlan + GatewayClass** — pattern-specific data-path; depends on TMM data-path subnets.
- **20-min CNEInstance + License polling** — ported from aws-gpu-setup/deploy-bnk.sh:248-end.
- **inspect / doctor / status polish** — currently they read TF state; need rewriting to read `.awsbnkctl/<cluster>/state.env` + tag-discovery.
- **Deletion of `terraform/` + `internal/tf/` + `embedded.go`** — biggest "cleanup" task; ~1480 LOC + binary fetch + embedded TF modules. Safe to delete once `inspect`/`doctor`/`status` are migrated.

## Open questions for the human

- Which slice 7 to pick: (a) CNEInstance + License + IRSA SA (the "BNK activation" slice — biggest user-visible value), (b) `inspect`/`doctor`/`status` polish (medium), or (c) `terraform/` cleanup (small, mostly deletes)?
- Live-test cluster config at `/tmp/syd-tracer-bnk.yaml` is **NOT** persisted — it's a temp-dir file with operator paths to `/Users/j.lucia/Code/aws-gpu-setup/cne_pull_64.json` + `/Users/j.lucia/Code/aws-gpu-setup/syd-test-lab.jwt`. Future live tests will need to rebuild it (or persist it to `examples/syd-tracer/cluster-local.yaml` which would need `.gitignore` addition).
