# EnvoyProxy CRD — vendored from Envoy Gateway v1.7.1
# Source: https://github.com/envoyproxy/gateway/releases/tag/v1.7.1
# crds/generated/gateway.envoyproxy.io_envoyproxies.yaml
# Applied by `awsbnkctl forge proxy prepare-envoy` before the EnvoyProxy CR,
# so the RESTMapper can resolve gateway.envoyproxy.io/v1alpha1/EnvoyProxy
# even if forge's gateway-helm install has not yet run.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: envoyproxies.gateway.envoyproxy.io
spec:
  group: gateway.envoyproxy.io
  names:
    categories:
    - envoy-gateway
    kind: EnvoyProxy
    listKind: EnvoyProxyList
    plural: envoyproxies
    singular: envoyproxy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EnvoyProxy is the schema for the envoyproxies API.
        properties:
          apiVersion:
            description: APIVersion defines the versioned schema of this representation
              of an object.
            type: string
          kind:
            description: Kind is a string value representing the REST resource this
              object represents.
            type: string
          metadata:
            type: object
          spec:
            description: EnvoyProxySpec defines the desired state of EnvoyProxy.
            properties:
              bootstrap:
                description: Bootstrap defines the Envoy Bootstrap that is applied
                  to the managed Envoy Proxy fleet.
                properties:
                  jsonPatches:
                    description: JSONPatches defines the JSONPatches to be applied
                      to the default Envoy Bootstrap config.
                    items:
                      description: JSONPatchOperation defines the JSON Patch Operation
                        as defined in https://www.rfc-editor.org/rfc/rfc6902
                      properties:
                        from:
                          description: From is the source location.
                          type: string
                        jsonValue:
                          description: JSONValue is the new value of the path location.
                          x-kubernetes-preserve-unknown-fields: true
                        op:
                          description: Op is the type of operation to perform
                          enum:
                          - add
                          - remove
                          - replace
                          - move
                          - copy
                          - test
                          type: string
                        path:
                          description: Path is the location of the target document/field
                            where the operation will be performed.
                          type: string
                        value:
                          description: Value is the new value of the path location.
                          x-kubernetes-preserve-unknown-fields: true
                      required:
                      - op
                      - path
                      type: object
                    type: array
                  type:
                    description: Type is the type of bootstrap configuration, a
                      Replace or Merge.
                    enum:
                    - Merge
                    - Replace
                    type: string
                  value:
                    description: Value is a YAML string of the bootstrap.
                    type: string
                required:
                - type
                type: object
              concurrency:
                description: Concurrency defines the number of worker threads to
                  run.
                format: int32
                type: integer
              extraArgs:
                description: ExtraArgs defines additional command line options that
                  should be passed to Envoy.
                items:
                  type: string
                type: array
              filterOrder:
                description: FilterOrder defines the order of filters in the Envoy
                  HTTP filter chain.
                items:
                  description: FilterPosition defines the position of an Envoy HTTP
                    filter in the filter chain.
                  properties:
                    after:
                      description: After defines the filter that should come after
                        the filter in the filterOrder.
                      enum:
                      - envoy.filters.http.cors
                      - envoy.filters.http.ext_authz
                      - envoy.filters.http.ext_proc
                      - envoy.filters.http.fault
                      - envoy.filters.http.jwt_authn
                      - envoy.filters.http.oauth2
                      - envoy.filters.http.rbac
                      - envoy.filters.http.wasm
                      type: string
                    before:
                      description: Before defines the filter that should come before
                        the filter in the filterOrder.
                      enum:
                      - envoy.filters.http.cors
                      - envoy.filters.http.ext_authz
                      - envoy.filters.http.ext_proc
                      - envoy.filters.http.fault
                      - envoy.filters.http.jwt_authn
                      - envoy.filters.http.oauth2
                      - envoy.filters.http.rbac
                      - envoy.filters.http.wasm
                      type: string
                    name:
                      description: Name of the filter.
                      enum:
                      - envoy.filters.http.cors
                      - envoy.filters.http.ext_authz
                      - envoy.filters.http.ext_proc
                      - envoy.filters.http.fault
                      - envoy.filters.http.jwt_authn
                      - envoy.filters.http.oauth2
                      - envoy.filters.http.rbac
                      - envoy.filters.http.wasm
                      type: string
                  required:
                  - name
                  type: object
                type: array
              logging:
                default:
                  level:
                    default: warn
                description: Logging defines logging parameters for managed Envoys.
                properties:
                  level:
                    additionalProperties:
                      description: LogLevel defines a log level for Envoy Gateway
                        and EnvoyProxy.
                      enum:
                      - debug
                      - info
                      - warn
                      - error
                      type: string
                    description: Level is a map of logging level per component,
                      where the component is the key and the log level is the value.
                    type: object
                type: object
              mergeGateways:
                description: MergeGateways defines if Gateway resources should
                  be merged onto the same Envoy Proxy Infrastructure.
                type: boolean
              namespaceLabels:
                description: NamespaceLabels resolves Gateway Namespaces from a
                  list of labels.
                items:
                  type: string
                type: array
              provider:
                description: Provider defines the desired resource provider and
                  provider-specific configuration.
                properties:
                  kubernetes:
                    description: Kubernetes defines the desired state of the Kubernetes
                      resource provider.
                    properties:
                      envoyDeployment:
                        description: EnvoyDeployment defines the desired state
                          of the Envoy deployment resource.
                        properties:
                          container:
                            description: Container defines the desired state of
                              the Envoy container resource.
                            properties:
                              env:
                                items:
                                  description: EnvVar represents an environment
                                    variable present in a Container.
                                  properties:
                                    name:
                                      description: Name of the environment variable.
                                      type: string
                                    value:
                                      description: Variable references $(VAR_NAME)
                                        are expanded using the previously defined
                                        environment variables in the container and
                                        any service environment variables.
                                      type: string
                                    valueFrom:
                                      description: Source for the environment
                                        variable's value.
                                      properties:
                                        configMapKeyRef:
                                          description: Selects a key of a ConfigMap.
                                          properties:
                                            key:
                                              description: The key to select.
                                              type: string
                                            name:
                                              description: Name of the referent.
                                              type: string
                                            optional:
                                              description: Specify whether the
                                                ConfigMap or its key must be defined
                                              type: boolean
                                          required:
                                          - key
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        fieldRef:
                                          description: Selects a field of the
                                            pod.
                                          properties:
                                            apiVersion:
                                              description: Version of the schema
                                                the FieldPath is written in terms
                                                of.
                                              type: string
                                            fieldPath:
                                              description: Path of the field to
                                                select in the specified API version.
                                              type: string
                                          required:
                                          - fieldPath
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        resourceFieldRef:
                                          description: Selects a resource of the
                                            container.
                                          properties:
                                            containerName:
                                              description: Container name.
                                              type: string
                                            divisor:
                                              anyOf:
                                              - type: integer
                                              - type: string
                                              description: Specifies the output
                                                format of the exposed resources.
                                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                              x-kubernetes-int-or-string: true
                                            resource:
                                              description: 'Required: resource
                                                to select'
                                              type: string
                                          required:
                                          - resource
                                          type: object
                                          x-kubernetes-map-type: atomic
                                        secretKeyRef:
                                          description: Selects a key of a secret
                                            in the pod's namespace
                                          properties:
                                            key:
                                              description: The key of the secret
                                                to select from.
                                              type: string
                                            name:
                                              description: Name of the referent.
                                              type: string
                                            optional:
                                              description: Specify whether the
                                                Secret or its key must be defined
                                              type: boolean
                                          required:
                                          - key
                                          type: object
                                          x-kubernetes-map-type: atomic
                                      type: object
                                  required:
                                  - name
                                  type: object
                                type: array
                              image:
                                description: Image specifies the EnvoyProxy container
                                  image to be used.
                                type: string
                              resources:
                                description: Resources required by this container.
                                properties:
                                  claims:
                                    description: Claims lists the names of resources,
                                      defined in spec.resourceClaims, that are used
                                      by this container.
                                    items:
                                      description: ResourceClaim references one
                                        entry in PodSpec.ResourceClaims.
                                      properties:
                                        name:
                                          description: Name must match the name
                                            of one entry in pod.spec.resourceClaims
                                            of the Pod where this field is used.
                                          type: string
                                        request:
                                          description: Request is the name chosen
                                            for a request in the referenced claim.
                                          type: string
                                      required:
                                      - name
                                      type: object
                                    type: array
                                    x-kubernetes-list-map-keys:
                                    - name
                                    x-kubernetes-list-type: map
                                  limits:
                                    additionalProperties:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    description: Limits describes the maximum amount
                                      of compute resources allowed.
                                    type: object
                                  requests:
                                    additionalProperties:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                    description: Requests describes the minimum
                                      amount of compute resources required.
                                    type: object
                                type: object
                              securityContext:
                                description: SecurityContext defines the security
                                  options the container should be run with.
                                properties:
                                  allowPrivilegeEscalation:
                                    description: AllowPrivilegeEscalation controls
                                      whether a process can gain more privileges
                                      than its parent process.
                                    type: boolean
                                  appArmorProfile:
                                    description: appArmorProfile is the AppArmor
                                      options to use by this container.
                                    properties:
                                      localhostProfile:
                                        description: localhostProfile indicates
                                          a profile loaded on the node that should
                                          be used.
                                        type: string
                                      type:
                                        description: type indicates which kind
                                          of AppArmor profile will be applied.
                                        type: string
                                    required:
                                    - type
                                    type: object
                                  capabilities:
                                    description: The capabilities to add/drop when
                                      running containers.
                                    properties:
                                      add:
                                        description: Added capabilities
                                        items:
                                          description: Capability represent POSIX
                                            capabilities type
                                          type: string
                                        type: array
                                      drop:
                                        description: Removed capabilities
                                        items:
                                          description: Capability represent POSIX
                                            capabilities type
                                          type: string
                                        type: array
                                    type: object
                                  privileged:
                                    description: Run container in privileged mode.
                                    type: boolean
                                  procMount:
                                    description: procMount denotes the type of proc
                                      mount to use for the containers.
                                    type: string
                                  readOnlyRootFilesystem:
                                    description: Whether this container has a read-only
                                      root filesystem.
                                    type: boolean
                                  runAsGroup:
                                    description: The GID to run the entrypoint of
                                      the container process.
                                    format: int64
                                    type: integer
                                  runAsNonRoot:
                                    description: Indicates that the container must
                                      run as a non-root user.
                                    type: boolean
                                  runAsUser:
                                    description: The UID to run the entrypoint of
                                      the container process.
                                    format: int64
                                    type: integer
                                  seLinuxOptions:
                                    description: The SELinux context to be applied
                                      to the container.
                                    properties:
                                      level:
                                        description: Level is SELinux level label
                                          that applies to the container.
                                        type: string
                                      role:
                                        description: Role is a SELinux role label
                                          that applies to the container.
                                        type: string
                                      type:
                                        description: Type is a SELinux type label
                                          that applies to the container.
                                        type: string
                                      user:
                                        description: User is a SELinux user label
                                          that applies to the container.
                                        type: string
                                    type: object
                                  seccompProfile:
                                    description: The seccomp options to use by
                                      this container.
                                    properties:
                                      localhostProfile:
                                        description: localhostProfile indicates
                                          a profile loaded on the node that should
                                          be used.
                                        type: string
                                      type:
                                        description: 'type indicates which kind
                                          of seccomp profile will be applied.'
                                        enum:
                                        - Localhost
                                        - RuntimeDefault
                                        - Unconfined
                                        type: string
                                    required:
                                    - type
                                    type: object
                                  windowsOptions:
                                    description: The Windows specific settings
                                      applied to all containers.
                                    properties:
                                      gmsaCredentialSpec:
                                        description: GMSACredentialSpec is where
                                          the GMSA admission webhook inlines the
                                          contents of the GMSA credential spec
                                          named by the GMSACredentialSpecName
                                          field.
                                        type: string
                                      gmsaCredentialSpecName:
                                        description: GMSACredentialSpecName is
                                          the name of the GMSA credential spec
                                          to use.
                                        type: string
                                      hostProcess:
                                        description: HostProcess determines if
                                          a container should be run as a 'Host
                                          Process' container.
                                        type: boolean
                                      runAsUserName:
                                        description: The UserName in Windows to
                                          run the entrypoint of the container
                                          process.
                                        type: string
                                    type: object
                                type: object
                              volumeMounts:
                                description: VolumeMounts are volumes to mount into
                                  the Envoy container's filesystem.
                                items:
                                  description: VolumeMount describes a mounting
                                    of a Volume within a container.
                                  properties:
                                    mountPath:
                                      description: Path within the container at
                                        which the volume should be mounted.
                                      type: string
                                    mountPropagation:
                                      description: mountPropagation determines how
                                        mounts are propagated from the host to container
                                        and the other way around.
                                      type: string
                                    name:
                                      description: This must match the Name of a
                                        Volume.
                                      type: string
                                    readOnly:
                                      description: Mounted read-only if true, read-write
                                        otherwise (false or unspecified).
                                      type: boolean
                                    recursiveReadOnly:
                                      description: RecursiveReadOnly specifies whether
                                        read-only mounts should be handled recursively.
                                      type: string
                                    subPath:
                                      description: Path within the volume from which
                                        the container's volume should be mounted.
                                      type: string
                                    subPathExpr:
                                      description: Expanded path within the volume
                                        from which the container's volume should
                                        be mounted.
                                      type: string
                                  required:
                                  - mountPath
                                  - name
                                  type: object
                                type: array
                            type: object
                          initContainers:
                            description: List of initialization containers belonging
                              to the pod.
                            items:
                              description: A single application container that you
                                want to run within a pod.
                              x-kubernetes-preserve-unknown-fields: true
                            type: array
                          patch:
                            description: Patch defines how to perform the patch
                              operation to the Envoy Deployment resource
                            properties:
                              type:
                                default: StrategicMerge
                                description: Type is the type of merge patch to
                                  apply to an object specification patch merge patch.
                                enum:
                                - StrategicMerge
                                - MergeAndOverride
                                type: string
                              value:
                                description: Object contains the raw configuration
                                  for merged object.
                                x-kubernetes-preserve-unknown-fields: true
                              required:
                              - value
                            type: object
                          pod:
                            description: Pod defines the desired state of the Envoy
                              pod resource.
                            properties:
                              affinity:
                                description: If specified, the pod's scheduling constraints.
                                x-kubernetes-preserve-unknown-fields: true
                              annotations:
                                additionalProperties:
                                  type: string
                                description: Annotations are the annotations to
                                  be written to the Pod.
                                type: object
                              imagePullSecrets:
                                description: ImagePullSecrets is an optional list
                                  of references to secrets in the same namespace
                                  to use for pulling any of the images used by
                                  this PodSpec.
                                items:
                                  description: LocalObjectReference contains enough
                                    information to let you locate the referenced
                                    object inside the same namespace.
                                  properties:
                                    name:
                                      description: Name of the referent.
                                      type: string
                                  type: object
                                  x-kubernetes-map-type: atomic
                                type: array
                              labels:
                                additionalProperties:
                                  type: string
                                description: Labels are the additional labels to
                                  be written to the Pod.
                                type: object
                              nodeSelector:
                                additionalProperties:
                                  type: string
                                description: NodeSelector is a selector which must
                                  be true for the pod to fit on a node.
                                type: object
                              securityContext:
                                description: SecurityContext holds pod-level security
                                  attributes and common container settings.
                                x-kubernetes-preserve-unknown-fields: true
                              terminationGracePeriodSeconds:
                                description: TerminationGracePeriodSeconds is the
                                  grace period seconds for the pod.
                                format: int64
                                type: integer
                              tolerations:
                                description: If specified, the pod's tolerations.
                                items:
                                  description: The pod this Toleration is attached
                                    to tolerates any taint that matches the triple
                                    <key,value,effect> using the matching operator
                                    <operator>.
                                  x-kubernetes-preserve-unknown-fields: true
                                type: array
                              topologySpreadConstraints:
                                description: TopologySpreadConstraints describes
                                  how a group of pods ought to spread across topology
                                  domains.
                                items:
                                  description: TopologySpreadConstraint specifies
                                    how to spread matching pods among the given
                                    topology.
                                  x-kubernetes-preserve-unknown-fields: true
                                type: array
                              volumes:
                                description: Volumes that can be mounted by containers
                                  belonging to the pod.
                                items:
                                  description: Volume represents a named volume
                                    in a pod that may be accessed by any container
                                    in the pod.
                                  x-kubernetes-preserve-unknown-fields: true
                                type: array
                            type: object
                          replicas:
                            description: Replicas is the number of desired pods.
                            format: int32
                            type: integer
                          strategy:
                            description: The deployment strategy to use to replace
                              existing pods with new ones.
                            x-kubernetes-preserve-unknown-fields: true
                        type: object
                      envoyHpa:
                        description: EnvoyHpa defines the Horizontal Pod Autoscaler
                          settings for Envoy Proxy Deployment.
                        x-kubernetes-preserve-unknown-fields: true
                      envoyPodDisruptionBudget:
                        description: EnvoyPodDisruptionBudget defines the settings
                          for the Envoy Proxy PodDisruptionBudget resource.
                        x-kubernetes-preserve-unknown-fields: true
                      envoyService:
                        description: EnvoyService defines the desired state of the
                          Envoy service resource.
                        properties:
                          allocateLoadBalancerNodePorts:
                            description: AllocateLoadBalancerNodePorts defines if
                              NodePorts will be automatically allocated for services
                              with type LoadBalancer.
                            type: boolean
                          annotations:
                            additionalProperties:
                              type: string
                            description: Annotations that should be appended to the
                              service.
                            type: object
                          externalTrafficPolicy:
                            description: ExternalTrafficPolicy determines the externalTrafficPolicy
                              for the Envoy Service.
                            enum:
                            - Cluster
                            - Local
                            type: string
                          loadBalancerClass:
                            description: LoadBalancerClass, when specified, allows
                              for filtering load balancer providers.
                            type: string
                          loadBalancerIP:
                            description: LoadBalancerIP defines the IP Address of
                              the underlying load balancer service.
                            type: string
                          loadBalancerSourceRanges:
                            description: LoadBalancerSourceRanges defines a list
                              of allowed IP addresses which are combined with the
                              IPTables rules to restrict access the load balancer.
                            items:
                              type: string
                            type: array
                          mergeType:
                            description: MergeType specifies the merge type for
                              the Service object.
                            enum:
                            - StrategicMerge
                            - JSONMerge
                            type: string
                          patch:
                            description: Patch defines how to perform the patch
                              operation to the EnvoyService resource
                            properties:
                              type:
                                default: StrategicMerge
                                description: Type is the type of merge patch to
                                  apply to an object specification patch merge patch.
                                enum:
                                - StrategicMerge
                                - MergeAndOverride
                                type: string
                              value:
                                description: Object contains the raw configuration
                                  for merged object.
                                x-kubernetes-preserve-unknown-fields: true
                            required:
                            - value
                            type: object
                          type:
                            description: Type defines the type of Service resource.
                            enum:
                            - LoadBalancer
                            - ClusterIP
                            - NodePort
                            type: string
                        type: object
                      useListenerPortAsContainerPort:
                        description: UseListenerPortAsContainerPort disables the
                          port shifting feature in Envoy Proxy.
                        type: boolean
                      watchNamespaces:
                        description: WatchNamespaces defines the namespaces the
                          EnvoyProxy watches.
                        items:
                          type: string
                        type: array
                    type: object
                  type:
                    description: Type is the type of resource provider to use.
                    enum:
                    - Kubernetes
                    type: string
                required:
                - type
                type: object
              routingType:
                description: RoutingType can be set to "Service" to use Service
                  Routing. Default is "Endpoint" routing.
                enum:
                - Service
                - Endpoint
                type: string
              shutdown:
                description: Shutdown defines configuration for graceful envoy shutdown
                  process.
                properties:
                  drainTimeout:
                    description: DrainTimeout defines the graceful drain timeout.
                    type: string
                  minDrainDuration:
                    description: MinDrainDuration defines the minimum drain duration
                      allowing time for endpoint deprogramming to complete.
                    type: string
                type: object
              telemetry:
                description: Telemetry defines telemetry parameters for managed
                  Envoys.
                x-kubernetes-preserve-unknown-fields: true
            type: object
          status:
            description: EnvoyProxyStatus defines the actual state of EnvoyProxy.
            x-kubernetes-preserve-unknown-fields: true
        type: object
    served: true
    storage: true
    subresources:
      status: {}
