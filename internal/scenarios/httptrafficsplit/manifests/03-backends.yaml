apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-a-index
  namespace: {{.Namespace}}
data:
  index.html: |
    backend-a
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-a
  namespace: {{.Namespace}}
  labels: {app: backend-a}
spec:
  replicas: 1
  selector:
    matchLabels: {app: backend-a}
  template:
    metadata:
      labels: {app: backend-a}
    spec:
      containers:
      - name: nginx
        image: nginx:1.27-alpine
        ports: [{containerPort: 80}]
        volumeMounts:
        - {name: html, mountPath: /usr/share/nginx/html}
        readinessProbe:
          httpGet: {path: /, port: 80}
          initialDelaySeconds: 1
          periodSeconds: 2
        resources:
          requests: {cpu: 10m, memory: 16Mi}
          limits:   {cpu: 100m, memory: 64Mi}
      volumes:
      - name: html
        configMap: {name: backend-a-index}
---
apiVersion: v1
kind: Service
metadata:
  name: backend-a
  namespace: {{.Namespace}}
spec:
  selector: {app: backend-a}
  ports:
  - {name: http, port: 80, targetPort: 80}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-b-index
  namespace: {{.Namespace}}
data:
  index.html: |
    backend-b
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-b
  namespace: {{.Namespace}}
  labels: {app: backend-b}
spec:
  replicas: 1
  selector:
    matchLabels: {app: backend-b}
  template:
    metadata:
      labels: {app: backend-b}
    spec:
      containers:
      - name: nginx
        image: nginx:1.27-alpine
        ports: [{containerPort: 80}]
        volumeMounts:
        - {name: html, mountPath: /usr/share/nginx/html}
        readinessProbe:
          httpGet: {path: /, port: 80}
          initialDelaySeconds: 1
          periodSeconds: 2
        resources:
          requests: {cpu: 10m, memory: 16Mi}
          limits:   {cpu: 100m, memory: 64Mi}
      volumes:
      - name: html
        configMap: {name: backend-b-index}
---
apiVersion: v1
kind: Service
metadata:
  name: backend-b
  namespace: {{.Namespace}}
spec:
  selector: {app: backend-b}
  ports:
  - {name: http, port: 80, targetPort: 80}
