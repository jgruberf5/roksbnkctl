apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: scn-route
  namespace: {{.Namespace}}
spec:
  parentRefs:
  - name: scn-gateway
  hostnames:
  - awsbnkctl.local
  rules:
  - matches:
    - path: {type: PathPrefix, value: /}
    backendRefs:
    - name: nginx
      port: 80
