apiVersion: v1
kind: ConfigMap
metadata:
  name: mv-a-index
  namespace: {{.Namespace}}
data:
  index.html: |
    multivip-backend-a
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mv-a
  namespace: {{.Namespace}}
  labels: {app: mv-a}
spec:
  replicas: 1
  selector:
    matchLabels: {app: mv-a}
  template:
    metadata:
      labels: {app: mv-a}
    spec:
      containers:
      - name: nginx
        image: nginx:1.27-alpine
        ports: [{containerPort: 80}]
        volumeMounts:
        - {name: html, mountPath: /usr/share/nginx/html}
        readinessProbe:
          httpGet: {path: /, port: 80}
          initialDelaySeconds: 1
          periodSeconds: 2
        resources:
          requests: {cpu: 10m, memory: 16Mi}
          limits:   {cpu: 100m, memory: 64Mi}
      volumes:
      - name: html
        configMap: {name: mv-a-index}
---
apiVersion: v1
kind: Service
metadata:
  name: mv-a
  namespace: {{.Namespace}}
spec:
  selector: {app: mv-a}
  ports:
  - {name: http, port: 80, targetPort: 80}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: mv-b-index
  namespace: {{.Namespace}}
data:
  index.html: |
    multivip-backend-b
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mv-b
  namespace: {{.Namespace}}
  labels: {app: mv-b}
spec:
  replicas: 1
  selector:
    matchLabels: {app: mv-b}
  template:
    metadata:
      labels: {app: mv-b}
    spec:
      containers:
      - name: nginx
        image: nginx:1.27-alpine
        ports: [{containerPort: 80}]
        volumeMounts:
        - {name: html, mountPath: /usr/share/nginx/html}
        readinessProbe:
          httpGet: {path: /, port: 80}
          initialDelaySeconds: 1
          periodSeconds: 2
        resources:
          requests: {cpu: 10m, memory: 16Mi}
          limits:   {cpu: 100m, memory: 64Mi}
      volumes:
      - name: html
        configMap: {name: mv-b-index}
---
apiVersion: v1
kind: Service
metadata:
  name: mv-b
  namespace: {{.Namespace}}
spec:
  selector: {app: mv-b}
  ports:
  - {name: http, port: 80, targetPort: 80}
