---
# HuggingFace token: mount the hf-token Secret if the model is gated.
# Create it with:
#   kubectl -n {{.Namespace}} create secret generic hf-token \
#     --from-literal=token=<YOUR_HF_TOKEN>
# If the model is public (no gated access needed), this Secret and the
# env var below can be omitted. The Secret is referenced optionally via
# envFrom so the Deployment starts regardless.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vllm
  namespace: {{.Namespace}}
  labels:
    app: vllm
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vllm
  template:
    metadata:
      labels:
        app: vllm
    spec:
      # Target GPU nodes: the GPU nodegroup is tainted nvidia.com/gpu=present:NoSchedule
      # and labelled awsbnkctl.io/gpu=true. Both nodeSelector and toleration are required.
      nodeSelector:
        awsbnkctl.io/gpu: "true"
      tolerations:
      - key: nvidia.com/gpu
        operator: Exists
        effect: NoSchedule
      containers:
      - name: vllm
        image: vllm/vllm-openai:latest
        args:
        - --model
        - meta-llama/Meta-Llama-3-8B-Instruct
        - --served-model-name
        - llama3
        - --port
        - "8000"
        ports:
        - containerPort: 8000
        env:
        - name: HF_TOKEN
          valueFrom:
            secretKeyRef:
              name: hf-token
              key: token
              # optional: true so the pod starts even without the Secret;
              # the model pull will fail at runtime if the model requires auth.
              optional: true
        - name: HUGGING_FACE_HUB_TOKEN
          valueFrom:
            secretKeyRef:
              name: hf-token
              key: token
              optional: true
        resources:
          requests:
            nvidia.com/gpu: "1"
            memory: 16Gi
            cpu: "4"
          limits:
            nvidia.com/gpu: "1"
            memory: 24Gi
            cpu: "8"
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 120
          periodSeconds: 15
          failureThreshold: 20
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 180
          periodSeconds: 30
          failureThreshold: 5
---
apiVersion: v1
kind: Service
metadata:
  name: vllm
  namespace: {{.Namespace}}
spec:
  selector:
    app: vllm
  ports:
  - name: http
    port: 80
    targetPort: 8000
