# Sprint 22

**Theme:** `mdbook` CI matrix expansion + `down` composite UX + `DetectShape` correctness. The original (2026-05-21 drafted) scope was a one-line edit to fold the `mdbook` Docker image into `.github/workflows/tools-images.yml` so it auto-builds + auto-pushes on `main` and tag pushes like its `ibmcloud` and `iperf3` siblings. On 2026-05-27 the integrator's demo.sh dry-run surfaced two staff-class bugs in the cluster/trial phase-split dispatch (PRD 06) and fixed them in-conversation; both shipped under this sprint's banner rather than spawning a parallel sprint. Validator + architect + tech-writer scopes still pending; staff scope is closure-only (no active work; both Issues shipped pre-dispatch).

_Drafted 2026-05-21 (CI scope) + 2026-05-27 (staff scope folded in). The CI gap surfaced when the `v1.7.0` `make release-publish` step 2 intermittently failed on Puppeteer/Chromium cold-start in the mdbook container — symptom of a stale image that the integrator was manually pushing. The two staff bugs surfaced when the integrator ran demo.sh to verify the (already-shipped) v1.7.0 cut against canada-roks: the composite `down` only destroyed the bnk phase (operator had to manually run `cluster down`), and after that fix the next `bnk down` errored with `"this workspace is legacy single-state"` because `DetectShape`'s heuristic mis-classified post-`up` trial states whose data sources sit under cluster-phase module prefixes._

## Integrator decisions baked in (do not relitigate)

1. **Three deliverable streams, one sprint.** The two staff Issues (DetectShape correctness, down-prompt UX) and the CI matrix work share no files and no scope tension — they ride together because they were surfaced and triaged in the same conversation. Don't try to refactor the staff work; it's shipped. Don't try to expand the CI work beyond the one matrix entry.
2. **Staff scope is closure-only.** Both Issues shipped before this sprint was formally dispatched (commits `cbb9c1b` for DetectShape, `18415eb` for down-prompt). The staff prompt in this dir is for audit/reproducibility, not active work. Mirrors inversely Sprint 20's "no architect deliverables (release-tooling-only)" precedent.
3. **Release tag is gated on Sprint 23.** A separate Issue (`issues/issue_sprint23_staff.md`) was filed at the same time documenting a phase-separation leak — a normal post-`up` trial state holds two managed cluster-shared resources (`module.testing.tls_private_key.jumphost_shared_key`, `module.roks_cluster.module.cluster.ibm_resource_instance.cos_instance`) that `bnk-phase-override.tfvars` fails to suppress. Pre-Sprint-22 the DetectShape misclassification masked this leak; post-fix it's a real `bnk down` resource-damage hazard. DO NOT tag a release at the end of this sprint — wait for Sprint 23 to ship and bundle both into one `v1.7.1` cut.
4. **No edits to RunE bodies or `tfstate.go` heuristic.** Staff already settled both. Don't second-guess the `mode == "managed"` + `type == "ibm_container_vpc_cluster"` filter or the `in.Auto = true` composite-confirmation flow.
5. **Live verify of DetectShape is gated on Sprint 23.** Per `live-verify-high-issues` memory the DetectShape fix is high-sev and needs a real `up → down` cycle to fully close. That cycle would currently hit the Sprint 23 phase-leak (orphan cluster-shared resources during `bnk down`), so the live verify waits. Tech-writer's launch verdict should call this out explicitly; do NOT treat the absence of live verify as a Sprint 22 blocker.

## Per-role scope

| Role | Scope |
|---|---|
| **Staff** Issue 1 (shipped, `cbb9c1b`) + Issue 2 (shipped, `18415eb`) | No active work. Closure-only documentation of (a) the `DetectShape` heuristic tightening in `internal/config/tfstate.go` (require `mode == "managed"` AND `type == "ibm_container_vpc_cluster"` AND a `clusterPhaseModules` prefix match) + new fixture + 2 new helper tests + 3 updated helper tests; (b) the `RunDown` composite Split-branch up-front-confirmation flow in `internal/orchestration/lifecycle.go` + cli-adapter mirror of `in.Auto` onto `flagAuto` in `internal/cli/lifecycle.go`. See `issues/issue_sprint22_staff.md` "Fix as shipped" section for full details. |
| **Validator** Issue 1 | Add `mdbook` to the `strategy.matrix.image` list in `.github/workflows/tools-images.yml` (alongside the existing `ibmcloud` and `iperf3` entries). Verify the existing `Build and push (tag)` / `Build and push (main)` / `Build and push (dispatch)` steps work for `mdbook` with `context: .` + `file: ./tools/docker/${{ matrix.image }}/Dockerfile`. The mdbook Dockerfile (`tools/docker/mdbook/Dockerfile`) doesn't reference repo-root paths so the broader context is harmless (same as `iperf3`'s situation per the existing workflow comment). Trigger a manual `workflow_dispatch` run after merge to verify the matrix expansion before relying on it for the next main-push or tag-push. |
| **Architect** Issue 1 (light) | One short note in `CONTRIBUTING.md` (or wherever the docker-image-publishing flow is documented — verify in `docs/PRD/03-*` or the contributor docs) confirming `mdbook` is now CI-managed; remove any "remember to manually push the mdbook image" callouts if they exist. |
| **Tech-writer** Issue 1 (light, runs after) | Drift sweep covering BOTH the CI work and the already-shipped staff fixes: (a) confirm the new mdbook matrix entry produced the expected `ghcr.io/jgruberf5/roksbnkctl-tools-mdbook:dev` push on the post-integration main push, spot-check the manifest digest matches what the workflow built; (b) verify `book/src/11-tearing-down.md` reflects the new combined down-prompt copy (already updated in `18415eb` — confirm no other chapters drifted); (c) GREEN/RED launch verdict explicitly noting the deferred live verify and Sprint 23 release-gate. |

## Constraints (binding on every role)

- `internal/orchestration/`, `internal/config/tfstate.go`, `internal/cli/lifecycle.go`, `internal/cli/cluster_phase.go` are **out of scope** for everyone except (already-done) staff — the heuristic fix and the composite-confirmation flow shipped and must not be relitigated.
- DO NOT tag a release at the end of this sprint. Sprint 23 (`issues/issue_sprint23_staff.md`) ships first; the combined v1.7.1 tag covers both sprints. Tech-writer's GREEN/RED verdict explicitly notes this hold.
- No edits to any pre-existing `_test.go` (parity discipline carries forward).
- Do not commit; integrator commits. Do not run `gh issue create`.
