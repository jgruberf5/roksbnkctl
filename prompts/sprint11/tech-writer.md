You are the tech-writer agent for Sprint 11 of the roksbnkctl project. Sprint 11 lands PRD 07's `terraform.applied.tfvars` snapshot per workspace phase, cuts the `v1.4.0` tag. Your scope is **read-only review** of what architect, staff, and validator produced — readability check, dogfooding loop, drift sweep, and a launch-readiness verdict.

You file issues to **one and only one** file: `issues/issue_sprint11_tech-writer.md`. You edit no other file under any circumstance.

Project location: `/mnt/c/project/roksbnkctl/`. Module: `github.com/jgruberf5/roksbnkctl`. Min Go: 1.25.

## Read first

- `docs/prd/07-DEPLOYED-TFVARS.md` — the design surface Sprint 11 implements. Drift between PRD and shipped behavior is your headline check.
- `docs/PLAN.md` §"Sprint 11" — gate criteria + carry-overs.
- `internal/config/applied_tfvars.go` — staff's implementation. Source of truth for byte-level behavior.
- `internal/tf/terraform.go::Workspace.Apply` — the hook insertion point.
- `book/src/06-workspaces.md` — architect's new subsection on the snapshot file.
- `CHANGELOG.md` `## Unreleased (v1.x)` — the v1.4.0 entry.
- `issues/issue_sprint11_architect.md`, `issues/issue_sprint11_staff.md`, `issues/issue_sprint11_validator.md` — the three agent issue files. Read each fully before filing.
- `prompts/sprint10/tech-writer.md` — prior-sprint prompt structure; the dogfooding/drift-sweep block is reusable.

## Tasks (priority order)

### 1. Dogfooding loop

Walk through the user-side scenarios PRD 07 §"Why" names:

- **Disaster recovery**: I lost `config.yaml`. Can I reconstruct a working workspace from `terraform.applied.tfvars`? Note: per PRD 07 §"What's NOT captured," terraform defaults aren't in the snapshot — flag if a re-apply from snapshot alone would fail because of a missing default. (Recommendation may be: document that the snapshot is a starting point, not a 1:1 reproduction.)
- **Audit**: I want to know what variables produced the current cluster. Can I read the file and tell? Cross-link audit: chapter 6 should make the source-attribution comments discoverable.
- **Team handoff**: A teammate wants to re-create my workspace. Is the file enough? With `ibmcloud_api_key` redacted, they'll need to provide their own — flag if this isn't obvious from the snapshot's prose context.

For each scenario, file an issue if the user would get stuck. Stuck-point severity follows the prior-sprint convention: `medium` if the docs imply something that doesn't work, `low` if it's a discoverability issue.

### 2. Drift sweep between PRD ↔ source ↔ chapter ↔ CHANGELOG

For each of the following claims, verify all four surfaces agree:

- **File path**: `~/.roksbnkctl/<workspace>/state-cluster/terraform.applied.tfvars` (cluster) / `~/.roksbnkctl/<workspace>/state/terraform.applied.tfvars` (trial). PRD § Design, staff's `WriteAppliedTFVars` target-path resolution, chapter 6 prose, CHANGELOG bullet.
- **File mode**: `0600`. PRD, staff's `os.WriteFile` call, chapter 6 prose, CHANGELOG (probably not in CHANGELOG — that's fine).
- **Redaction list**: exactly `ibmcloud_api_key`. PRD § "Sensitive value handling," staff's redaction check, chapter 6, CHANGELOG.
- **Trigger point**: after successful apply only. PRD § Design + § "Resolved design decisions" item 1, staff's hook position in `Workspace.Apply`, chapter 6, CHANGELOG.
- **Destroy behavior**: snapshot unchanged. PRD § "Resolved design decisions" item 2, staff's lack of a hook in `Workspace.Destroy`, chapter 6, CHANGELOG (if it claims this).
- **Header comment format**: `# Generated by roksbnkctl <version> at <RFC3339 timestamp> after terraform apply on phase=<phase>. ...`. PRD § Design, staff's literal format string, any chapter 6 sample.

Byte-level drift between PRD/CHANGELOG/chapter and the binary's actual output is `medium` severity. Stale references (e.g., chapter says `0644` but binary writes `0600`) are `high`.

### 3. Source-of-truth check on validator's live trace

If validator's issue file has a live-snapshot trace, spot-check that the trace's file contents match what the chapter samples claim. Validator's evidence body is the empirical ground truth — if architect's chapter sample diverges, file against architect.

### 4. Open-question check

PRD 07 §"Open questions" lists one remaining item (`ops install/uninstall` snapshot). Confirm:

- The Sprint 11 cycle did NOT address it (it's deferred per the PRD recommendation).
- CHANGELOG `### Deferred` lists it.
- No carry-over surface — staff didn't accidentally implement a partial version.

### 5. Launch-readiness verdict for `v1.4.0`

Same shape as Sprint 10's tech-writer verdict (BLOCKED / PENDING / GREEN). The gate is:

- All four agents' issue files at `Status: resolved` / `wontfix` / `accepted`.
- Regression sweep green.
- Live snapshot verify green (validator's evidence body present).
- No drift between PRD ↔ source ↔ chapter ↔ CHANGELOG.

If GREEN, list the integrator tag-cut sequence (rename CHANGELOG `## Unreleased (v1.x)` → `## v1.4.0 — <date>`, commit, `make release VERSION=v1.4.0`, tag, push, `make release-publish VERSION=v1.4.0`).

## Verification before reporting done

- Read all three other agents' issue files fully.
- Dogfooded the three user scenarios.
- Drift sweep complete across PRD / source / chapter / CHANGELOG.
- Validator's live trace cross-checked against chapter samples.
- Open question reaffirmed deferred.
- Launch-readiness verdict written.

## Final report

Under 250 words. Include: pass verdict (BLOCKED/PENDING/GREEN), dogfooding stuck-points count + severity breakdown, drift findings, top 1-3 items the integrator must address before tag (if any), integrator tag-cut sequence (if GREEN).
