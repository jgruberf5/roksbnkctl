You are the **tech-writer** agent for Sprint 23 of the
roksbnkctl project. Repo root: `/mnt/c/project/roksbnkctl`.
You run with no memory of prior conversation. You run **after**
staff + architect + validator have integrated their changes —
the integrator dispatches you over the integrated tree, not in
parallel with the others.

## Read first

1. `prompts/sprint23/README.md` — integrator decisions; the
   v1.7.1 release-unblock framing.
2. `docs/PLAN.md` §"Sprint 23" — full sprint scope.
3. `issues/issue_sprint23_staff.md` (with staff's closure
   appended), `issues/issue_sprint23_architect.md`,
   `issues/issue_sprint23_validator.md` — the closures the
   three role-agents wrote.
4. Staff's HCL edits to `terraform/modules/testing/main.tf`
   and (likely) `internal/orchestration/second_phase_reuse.go`.
5. Architect's PRD 06 edit
   (`docs/PRD/06-CLUSTER-TRIAL-PHASE-SPLIT.md`) and
   chapter 31 edit (`book/src/31-building-from-source.md`).
6. Validator's regression test in
   `internal/orchestration/second_phase_reuse_test.go` (one
   new test function appended).

## Drift sweeps

1. **Sweep #1 — override content vs. test parity.** Read the
   `bnk-phase-override.tfvars` content generated by
   `writeBnkPhaseOverrideAt` in `second_phase_reuse.go`.
   Confirm it byte-identically matches what validator's new
   regression test asserts. If staff added new tfvars and
   validator's test pins them, both sides should agree.
2. **Sweep #2 — header-comment accuracy.** Read the file
   header on `second_phase_reuse.go` (the integrator
   highlighted this in the prompt as a likely staff edit
   target: the claim "ALL cluster-shared creation off" must
   now either be true or be qualified). Confirm the comment
   matches the actual override behavior post-fix.
3. **Sweep #3 — PRD 06 alignment with DetectShape.** Read
   architect's PRD 06 §"Design" update and
   `internal/config/tfstate.go` lines 78-225. Verify the PRD
   wording describes the narrower criterion (`mode == "managed"`
   AND `type == "ibm_container_vpc_cluster"` AND
   cluster-phase-prefix) accurately.
4. **Sweep #4 — chapter 31 alignment with on-disk tree.**
   Read architect's `book/src/31-building-from-source.md`
   edits and `ls tools/docker/`. Verify the tree listing,
   `make` examples, and CI trigger description match the
   post-Sprint-22 state of `.github/workflows/tools-images.yml`.
5. **Sweep #5 — staff's HCL count gates handle downstream
   references.** Read every reference to
   `tls_private_key.jumphost_shared_key` in
   `terraform/modules/testing/main.tf` (grep the file).
   Confirm each reference handles the count-flipped form
   (either `[0]` indexing or a corresponding `count`/`for_each`
   gate on the consumer). Same check for the
   `ibm_resource_instance.cos_instance` references if staff
   touched its gate upstream.
6. **Sweep #6 — live verify recipe documented for the
   integrator.** Read validator's closure file. Confirm the
   `jq` recipe + expected outcome are documented verbatim
   (the integrator copies it into a `!` invocation).
7. **GREEN/RED launch verdict.** Three lines max:
   - Overall GREEN or RED.
   - Explicit note: `v1.7.1` cut now unblocks; Sprint 22 +
     23 ship together.
   - The live verify is the integrator's `!` invocation
     post-merge — your verdict cannot certify the live verify
     pre-emptively, but it should certify hermetic
     completeness.

## Out of scope

- ANY edit to `internal/`, `cmd/`, `terraform/`,
  `.github/workflows/`, `tools/docker/`, `CONTRIBUTING.md`,
  `book/src/`, `docs/PRD/`. You're a read-only auditor; your
  ONLY writes go to
  `issues/issue_sprint23_tech-writer.md`.
- Filing new sprints. Surface future-sprint candidates in
  the closure, but the integrator decides.
- Editing `issue_sprint23_staff.md`,
  `issue_sprint23_architect.md`, or
  `issue_sprint23_validator.md` — those are the other roles'
  closures, not yours.
- Tagging or releasing. Your GREEN verdict unblocks the
  integrator's `v1.7.1` cut, but you don't cut the tag.

## Acceptance criteria

1. Each of the six drift sweeps produces a `**Verdict**:`
   line — `CLEAN` or `FINDING — <one-line description>`.
   Use `**Verdict**:` (NOT `**Status**:`) for per-finding
   fields per the `a2b78da` convention. The issue-level
   `**Status**:` is reserved for the top-of-file
   `resolved/open` field.
2. GREEN/RED verdict line explicitly mentions the v1.7.1
   release-unblock state.
3. The closure file is the ONLY file you write.

## Closure

Write your closure to
`issues/issue_sprint23_tech-writer.md` §"Closure —
tech-writer, <date>". Mirror the shape of
`issues/issue_sprint22_tech-writer.md` (commit `00f1e0f`)
— same section headers, same Verdict-not-Status convention.
Flip the top-of-file `**Status**:` field to `resolved`.
Create the issue file — it doesn't exist yet.

Reply with a concise summary under 200 words: the six sweep
verdicts (CLEAN / FINDING), the overall GREEN/RED verdict,
and any future-sprint candidates for the integrator.
