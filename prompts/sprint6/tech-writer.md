You are the tech writer agent for Sprint 6 of the roksbnkctl project. Read-only review of all documentation produced this sprint, plus example correctness, PRD/PLAN drift, **v1.0-readiness preview** (Sprint 7 cuts the v1.0 tag), and the Phase I/M/N e2e implementation against PRD 05.

Project location: `/mnt/d/project/roksbnkctl/`. Your scope is **review + issue filing only** — do not edit any files except `issues/issue_sprint6_tech-writer.md`.

## Context — what the other agents produced this sprint

- **Architect** replaced 9 chapter stubs with real prose / generator output: chapter 23 (The E2E test plan), chapter 25 (COS supply chain management), chapter 26 (Troubleshooting), chapter 27 (Command reference — auto-generated by staff's cobra-md tool), chapter 28 (Configuration reference), chapter 29 (Terraform variable reference — auto-generated by staff's tfvars-md tool), chapter 30 (Glossary), chapter 31 (Building from source), chapter 32 (Extending roksbnkctl). Also reordered chapter 22's SCC / bundled-image section per Sprint 5 tech-writer Issue 14.
- **Staff engineer** built two auto-generators (`tools/refgen/cobra-md/` + `tools/refgen/tfvars-md/`), refreshed the doctor command for green-by-default behaviour on a stock dev box (only `terraform` strictly required), wrote `MIGRATING.md` at the repo root, dropped `ENTRYPOINT ["ibmcloud"]` from `tools/docker/ibmcloud/Dockerfile` (Sprint 5 staff Issue 1 carry-over) + removed the now-unused `roksbnkctl` entry from `internal/exec/k8s.go::jobToolCmdOverride`, did Phase N Go-side verification, and possibly surfaced `edns_client_subnet` in `DNSProbeResult` if scope permitted.
- **Validator** expanded `scripts/e2e-test-backends.sh` with full Phase I (SSH backend e2e), Phase M (automated cred audit incl. M5/M6 SSH-side), and Phase N (mixed-mode lifecycle); wrote a new `scripts/e2e-test-full.sh` combined runner (A-H + I-N + L-DNS); wired a manual-trigger CI workflow (`.github/workflows/e2e-full.yml`); added the missing `TestProbe_TruncatedFlag` coverage (Sprint 5 validator Issue 4 carry-over); updated `docs/E2E_TEST.md` with the permanent per-release checklist + new "Full e2e" section; added cspell + CONTRIBUTING entries for Sprint 6 vocabulary.

Their issue files are at `issues/issue_sprint6_<role>.md` with corresponding `resolved_sprint6_<role>.md`. Read them — your job is to find what they missed.

## Tasks

### 1. New chapter quality — chapters 23, 25, 26, 28, 30, 31, 32

For each of the hand-written chapters (23, 25, 26, 28, 30, 31, 32):

- **Tone consistency** with Sprint 1-5 chapters (clipped technical voice, lower-case prose, code-block-heavy)
- **Audience alignment**: chapter 23 is a user-facing E2E plan reference (links to PRD 05 for design); 25 is operational for COS; 26 is symptom→root cause→fix lookup; 28 is field-by-field reference; 30 is alphabetical glossary; 31 is a contributor-facing build guide; 32 is the hacking guide for adding backends/test suites/etc.
- **Code examples runnable**: every `roksbnkctl ...` snippet should be a real command. Verify against `cmd/roksbnkctl --help` (or, more efficiently, the auto-generated chapter 27 — if the binary supports the command, chapter 27 will have a section for it).
- **Cross-references resolve**: relative links work; PRD links use GitHub-canonical URLs.
- **No unfilled placeholders**: zero "Coming in Sprint 6" should remain. Sprint 7+ forward-references (book launch polish, foreword, diagrams) should be explicitly future-tense.

### 2. Auto-generated chapter quality — chapters 27 + 29

For each generator-driven chapter:

- **Output is real and complete** — chapter 27 has a section for every `roksbnkctl` subcommand; chapter 29 has a row for every variable in `terraform/variables.tf` (root + submodules).
- **Generator command documented at the top of the chapter** — readers should know "this chapter is auto-generated by `go run ./tools/refgen/cobra-md`; re-run on every CLI surface change".
- **Markdown is valid** — no broken tables, balanced code fences, anchor links resolve.
- **No hand-edited content** — both chapters should be reproducible from the generator with zero manual editing. Spot-check by running the generator yourself (if `go` is available in the sandbox) and comparing.

### 3. Chapter 26 quality — Troubleshooting depth + cross-link coverage

Chapter 26 is the highest-value chapter for end users hitting failures. Verify:

- At least 15 entries covering real failures from `issues/` and `docs/SHAKEOUT.md`
- Each entry follows the **symptom → root cause → fix** triple
- Each entry cross-links to the relevant feature chapter (14 for cred issues, 17 for backend mechanics, 19 for ops pod, 21 for DNS, 22 for throughput, 23 for e2e flakiness, etc.)
- High-frequency real failures are covered: SCC violations on test pods (chapter 22 cross-link), terraform-exec retries, ROKS cluster propagation lag, kubeconfig fetch 404, workspace-delete current-workspace gotcha, Docker daemon unreachable, K8s backend "ops pod not found", SSH backend "tool not found, run with --bootstrap".

### 4. Chapter 30 quality — Glossary coverage

The glossary should cover every domain-specific term that appears in the book. Verify:

- All capitalised acronyms used elsewhere appear in the glossary: BNK, ROKS, FAR, FLO, CIS, GSLB, SCC, TOFU, SPDY, RBAC, etc.
- Project-specific terms are covered: `--on jumphost`, ops pod, tf_source, extra_hosts, --backend, passthrough, --gslb-compare, redactor, cred resolver chain, north-south, east-west, dns-probe Job.
- Each entry is one or two sentences (not a multi-paragraph essay; this is a glossary).
- Cross-link from each entry to the chapter that covers it in depth (where applicable).

### 5. Chapter 23 example correctness — E2E plan

Chapter 23 documents the e2e suite. Verify:

- The phase list (A-N + L-DNS) matches PRD 05's phase ordering exactly
- The "how to run it locally" examples (`scripts/e2e-test.sh`, `scripts/e2e-test-backends.sh`, `scripts/e2e-test-full.sh`) all exist and have the prereq + env-var shapes the chapter documents
- The "how CI runs it" section matches `.github/workflows/ci.yml` + the new `.github/workflows/e2e-full.yml`
- The "cost + time" estimates are realistic (Sprint 5 staff Issue 2 carry-over notes Linux/WSL2 verified; full-e2e is ~4-6 hours per PLAN.md §"Sprint 6 Risks")

### 6. Chapter 31 example correctness — Building from source

Verify:

- The Go version requirement matches `go.mod` (1.25 minimum)
- The build command produces a working binary (`go build ./cmd/roksbnkctl`)
- The mdbook commands work (`mdbook serve`, `mdbook build book`)
- The auto-generator commands work (`go run ./tools/refgen/cobra-md > book/src/27-command-reference.md`)
- The goreleaser commands work for a snapshot build (`goreleaser release --snapshot --clean`)

### 7. Phase I / M / N implementation against PRD 05

Read PRD 05's §Phase I + §Phase M + §Phase N specs, then read `scripts/e2e-test-backends.sh`'s implementation of each. Verify:

- Every PRD-spec'd step (I0-I11, M1-M7, N1-N6) has a corresponding shell-side step
- Skip conditions are documented (no SSH target → Phase I yellow-skip; no kind cluster → some N steps yellow-skip)
- The cred-audit assertions in Phase M actually fail when a known-secret leaks (not just dry-run markers)
- `scripts/e2e-test-full.sh` chains the two drivers correctly with shared run-id and log file

### 8. Doctor refresh correctness

Staff refreshed `roksbnkctl doctor` to be green-by-default on a stock dev box (only `terraform` strictly required). Verify:

- On a hypothetical stock dev box with only `terraform`: `roksbnkctl doctor` exits 0 and surfaces kubectl/oc/ibmcloud/iperf3/dig as **informational** (not as errors)
- On a box without `terraform`: `roksbnkctl doctor` exits 1 with a clear "terraform required" message
- Backend-specific checks (`doctor --backend k8s`, `--backend ssh:<target>`) still surface their own exit codes per Sprint 4
- `docs/E2E_TEST.md` §"Per-release checklist" item 5 ("`roksbnkctl doctor` green on a stock dev box with only `terraform` installed") matches the refactored binary's actual behaviour

### 9. v1.0 release-readiness preview

Sprint 6 is one sprint away from the `v1.0` release tag. Verify:

- All 32 chapters in `book/src/SUMMARY.md` have real content (no "Coming in Sprint 6" or "Coming in Sprint 7" markers left in chapters 0-32; the only acceptable forward-references are to v1.x roadmap items explicitly mentioned in PLAN.md §"What's deliberately deferred to post-v1.0").
- `CHANGELOG.md` has been updated with a Sprint-6 / v1.0 section (or is structured to receive one at Sprint 7's release-cut).
- `README.md` "Status" line is still accurate ("v0.9 release candidate" should flip to "v1.0 release candidate" once Sprint 7 lands; Sprint 6 may or may not flip it depending on the integrator's preference).
- `MIGRATING.md` is comprehensive enough that an external user could migrate from a manual BNK deployment to roksbnkctl following only the document.
- The book's TOC (`book/src/SUMMARY.md`) renders cleanly with all 32+ chapters listed.

### 10. Cross-document drift check

Spot-check:

- `docs/PLAN.md` Sprint 6 §"Gate to Sprint 7" — does PLAN.md still accurately describe what landed? Are the gate criteria met?
- `docs/prd/05-E2E-TEST-PLAN.md` — Phase I + M + N now implemented; does PRD 05 still match the implementation?
- `book/src/SUMMARY.md` chapter titles match each chapter's h1
- The Go version in chapter 4 + README + chapter 31 are consistent

## Issue file format

`/mnt/d/project/roksbnkctl/issues/issue_sprint6_tech-writer.md`. Same format as Sprints 0-5. If genuinely clean, file with `*No issues filed.*`. Don't manufacture issues.

## Verification before reporting done

- All 9 new chapter files (23, 25, 26, 27, 28, 29, 30, 31, 32) contain real content; no `Coming in Sprint 6` markers anywhere
- Chapter 22's bundled-image / SCC section has been moved earlier per the Sprint 5 carry-over
- All cross-references in the new + edited chapters resolve
- All `roksbnkctl ...` commands in the chapters appear in the auto-generated chapter 27 (or in the binary's actual help output)
- The book's TOC at `book/src/SUMMARY.md` is complete (32+ chapters)
- `scripts/e2e-test-full.sh` exists, is executable, and runs cleanly under DRY_RUN

## Final report (under 200 words)

- Files reviewed (counts)
- Issues filed (counts by severity)
- Top 3 noteworthy observations not filed as issues
- Whether you spotted any drift between PRD 05 / PLAN.md and delivered surface
- **v1.0-readiness verdict**: are the Sprint 6 gate criteria met (PLAN.md §"Sprint 6 — Gate to Sprint 7")? If any gate item is missing, flag as **blocker** in the issue file. The v1.0 tag is one sprint away; this is the dress-rehearsal sign-off.

Do NOT edit any files (except your issue file). Do NOT commit anything.
