# The cluster phase (cluster up/down)

*Coming in Sprint 2.*

*The `roksbnkctl cluster up` / `cluster down` lifecycle: what the cluster phase deploys (ROKS cluster, cert-manager, jumphost), how its state is segregated under `state-cluster/`, and when you would run it independently of the BNK phase.*
