# Throughput testing

*Coming in Sprint 5.*

*iperf3 throughput testing internalized via the k8s backend: LoadBalancer vs ClusterIP modes, when a host iperf3 install is still useful (north-south from outside the cluster), and how to interpret the JSON results.*
