# DNS testing for GSLB

*Coming in Sprint 5.*

*The flagship testing chapter. The GSLB problem statement, why per-vantage probing matters, the `--server` / `--type` / `--iterations` flags, the `--gslb-compare` workflow, the JSON output schema, and worked F5 BIG-IP Next GSLB scenarios with expected divergence.*
