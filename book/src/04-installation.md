# Installation

*Coming in Sprint 1.*

*Single-binary install via `curl`, the apt repository, or `go install`. OS support matrix (Linux + macOS first-class, Windows compile-only). Verifying the install with `roksbnkctl --version` and `roksbnkctl doctor`.*
