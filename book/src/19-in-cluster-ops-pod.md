# The in-cluster ops pod

*Coming in Sprint 4.*

*`roksbnkctl ops install/show/uninstall`: what gets deployed (namespace, service account, ClusterRole, RoleBinding, projected secret), the RBAC privileges granted, and the rotation/refresh story for long-running deployments.*
