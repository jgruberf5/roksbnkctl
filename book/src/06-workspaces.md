# Workspaces

*Coming in Sprint 2.*

*Kubectl-style multi-environment isolation under `~/.roksbnkctl/<name>/`. Creating, switching, listing, and deleting workspaces; the parking-lot pattern used in the end-to-end tests; safe override with `-w`.*
