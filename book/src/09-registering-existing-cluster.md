# Registering an existing cluster

*Coming in Sprint 2.*

*`roksbnkctl cluster register <name>`: how to wire `roksbnkctl` up to a ROKS cluster you already operate, how COS instance discovery works, and when this is the right choice over `cluster up`.*
