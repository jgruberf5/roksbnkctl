# Credentials and the resolver chain

*Coming in Sprint 3.*

*How `IBMCLOUD_API_KEY` is resolved (env → keychain → config-b64 → prompt), how `kubeconfig` is discovered, where SSH key sources come from, and what is safe to commit to a workspace versus what must stay in your secret store.*
