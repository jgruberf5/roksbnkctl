# Workspace config (config.yaml)

*Coming in Sprint 3.*

*Full schema reference for the per-workspace `config.yaml` with an annotated example, every field documented, and the defaults applied when a key is omitted.*
