# Terraform variables (terraform.tfvars)

*Coming in Sprint 3.*

*The `terraform.tfvars` surface, the `--var-file` layering rule that lets workspace overrides win over defaults, and how `roksbnkctl tfvars` bootstraps a starter file from current workspace state.*
