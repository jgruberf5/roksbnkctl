# SSH targets

*Coming in Sprint 3.*

*The `targets:` block in workspace config: defining named SSH targets, the `tf-output:<name>` key source for keys produced by Terraform, agent integration, and host-key trust-on-first-use behavior.*
