# Troubleshooting

*Coming in Sprint 6.*

*Common failure modes from real deployments, organized as symptom → root cause → fix: terraform-exec retries, ROKS cluster propagation lag, kubeconfig fetch 404s, OpenShift SCC violations on test pods, and the `cluster down` → `workspace delete` current-workspace gotcha.*
