# Execution backends: local, docker, k8s, ssh

*Coming in Sprint 3.*

*Note: Sprint 3 lands the introduction (overview of the four backends and the `--backend` flag); Sprint 4 expands the chapter with deep-dive sections on each backend's internals.*

*The four execution backends `roksbnkctl` supports for running tools like `ibmcloud` and `iperf3`: how each one is wired, when to pick it, and the `--backend` flag that selects between them.*
