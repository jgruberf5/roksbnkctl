# Day-2 ops: status, logs, k get/apply/exec

*Coming in Sprint 2.*

*The internalized day-2 verbs: `roksbnkctl k get/apply/logs/exec/port-forward` for working against the cluster without `kubectl` on PATH, plus the `kubectl` and `oc` passthroughs that remain available as escape hatches.*
