# The --on flag and SSH jumphosts

*Coming in Sprint 1.*

*The persistent `--on <target>` flag that re-runs any `roksbnkctl` passthrough verb (kubectl, oc, ibmcloud, exec, shell) on a remote SSH host. Auto-population of `targets.jumphost` after `cluster up` from Terraform outputs.*
