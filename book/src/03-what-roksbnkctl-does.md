# What roksbnkctl does (and doesn't do)

*Coming in Sprint 1.*

*The tool's scope, the explicit non-goals (it is not a generic IBM Cloud CLI, not a generic Kubernetes CLI), and the relationship between the binary and the bundled Terraform that ships in the same release.*
